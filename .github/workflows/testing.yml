on: [push]

jobs:
  test-action:
    runs-on: ubuntu-latest
    name: Testing action
    steps:
    - name: Check commits in remote repo
      uses: ./
      with:
        token: ${{ secrets.GITHUB_TOKEN }}
        owner: 'sveltejs'
        repo: 'kit'
        dir: 'documentation'
        days: 3
